import { FilterCoinsPipe } from './filter-coins.pipe';

describe('FilterCoinsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoinsPipe();
    expect(pipe).toBeTruthy();
  });
});
